export default function generatePart5() {
  const base = ['W', 'q', 'k', '_', 'q', 't', 'z'];
  const reordered = [base[0], base[2], base[1], base[3], base[4], base[5], base[6]];
  return reordered.join(''); 
}
